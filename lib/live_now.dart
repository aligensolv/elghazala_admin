import 'package:flutter/material.dart';

class live_now extends StatelessWidget {
  const live_now({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}